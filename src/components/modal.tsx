import { Body, Header, Main, ModalCard } from '@/context/modal-context'

const Modal = () => {
  return (
    <Main>
      <ModalCard>
        <Header />
        <Body />
      </ModalCard>
    </Main>
  )
}

export default Modal
